<h1>test</h1>
{{#each shoppingCart.items as |item|}}
<div class="row">
  <div class="col-sm-4">
    <div class="well">
      <h4>{{item.title}}</h4>
      <p class="text-muted">{{item.price}}</p>
    </div>
  </div>
</div>
{{/each}}
