
import { inventoryIcon } from 'shop-s-mart/helpers/inventory-icon';
import { module, test } from 'qunit';

module('Unit | Helper | inventory icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inventoryIcon([42]);
  assert.ok(result);
});

